import db from '../models/index';


export const getDiscount = () => {
    return new Promise((resolve, reject) => {

        try {
            
        } catch (error) {
            
        }
     });
}